
fun main(args: Array<String>) {

/*
    val curve = mutableListOf<geoModel.InterpolatedBspline>()
    curve.add(geoModel.InterpolatedBspline(3))
    curve[0].addPts(linearAlgebra.Vector3(3.0, 3.0, 0.0))
    curve[0].addPts(linearAlgebra.Vector3(1.0, 1.0, 0.0))
    curve[0].addPts(linearAlgebra.Vector3(0.5, 0.5, 0.0))
    curve[0].addPts(linearAlgebra.Vector3().zero)
    println("curvePoint(t=0.5) : x = ${curve[0].curvePoint(0.5).x}, y = ${curve[0].curvePoint(0.5).y}")
    println("curveDers1(t=0.5) : x = ${curve[0].curveDers(0.5,1)[1].x}, y = ${curve[0].curveDers(0.5,1)[1].y}")

    val oldVector = linearAlgebra.Vector3().unitZ
    println("oldVector[2]=${oldVector[2]}")
    val mm = linearAlgebra.Matrix3().identity
    println("det=${mm.determinant}")
    val nn = linearAlgebra.Matrix(3).identity
    println("det=${nn.determinant}")
    val ll = linearAlgebra.Matrix(mm.m)
    println("det=${ll.determinant}")*/

    val mainFrame = MainFrame()

}